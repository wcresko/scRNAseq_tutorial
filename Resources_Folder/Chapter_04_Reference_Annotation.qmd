---
title: "Chapter 4 — Reference-based Annotation"
author: "Single Cell RNA-seq Workshop"
format:
  html:
    toc: true
    toc-depth: 3
    code-fold: false
    code-overflow: wrap
    highlight-style: github
    embed-resources: true
execute:
  eval: false
  echo: true
  warning: false
  message: false
editor: visual
---

::: callout-note
**Where this chapter sits.** Companion to **[Lecture 04](../Lecture_Folder/Lecture_04_Reference_Annotation.html)** and **[Tutorial 04](../Exercise_Folder/Tutorial_04_Reference_Annotation.html)**. **Prerequisites:** [Chapter 3 — Manual Annotation](Chapter_03_Markers_Annotation.html) (the complement to this).
:::

## 4.1 The two paradigms

Cell-type labeling has two methodological camps:

**Manual annotation (Chapter 3)** — inspect canonical markers per cluster, assign names by hand. Interpretable, biased by the annotator's experience, requires a literature.

**Reference-based annotation (this chapter)** — map your cells against a *labeled reference dataset* using a statistical method. Reproducible, biased by the reference's coverage, requires a reference of the same tissue.

Best practice is to run **both** and **reconcile disagreements** — they catch different kinds of errors. Manual annotation catches references missing your cell type; reference-based annotation catches missed subdivisions and reduces annotator bias. Automated annotation is now a crowded field — large comparisons evaluate 20–30+ classifiers and find that the best correlation- and classifier-based methods are accurate when the reference matches the query, but degrade sharply on cell types absent from the reference [@abdelaal2019comparison; @pasquini2021automated].

The workshop tutorial uses **Azimuth** as its reference-based method: it ships curated, atlas-quality references (including a PBMC reference that is a perfect fit for `ifnb`), downloads them on demand, and needs no separate Bioconductor reference-data packages. §4.4 sketches the two other paradigms — correlation (SingleR) and classifier (CellTypist) — you'll meet in the literature.

## 4.2 Azimuth — anchor-based reference mapping

Azimuth wraps Seurat's anchor-based mapping [@hao2021integrated] (the same method as Seurat integration, applied with a query → reference orientation). Pre-built atlas-quality references are available:

| Reference | Tissue | Source |
|---|---|---|
| `pbmcref` | PBMCs | Hao et al. 2021 (Seurat v4 paper) |
| `motorcortexref` | mouse cortex | Allen Brain Atlas |
| `fetusref` | human fetal | HCA Fetal Atlas |
| `lungref` | adult lung | HCA Lung Cell Atlas |
| (more) | various | atlas consortia |

For each query cell, Azimuth:

1. Projects it into the reference's PC space using shared HVGs
2. Finds its anchored partner cells in the reference (mutual nearest neighbors after L2 normalization)
3. Transfers the anchor partners' labels (with confidence weights)
4. Reports a **mapping score** per cell — how well the cell projected into the reference manifold

```r
library(Azimuth)
seu <- RunAzimuth(seu, reference = "pbmcref")
# new metadata: predicted.celltype.l1 / l2 / l3 (+ .score) and mapping.score
```

Strengths:

- Very fast (the reference is precomputed) and needs no Bioconductor reference-data download
- Hierarchical labels (`predicted.celltype.l1`, `l2`, `l3`) — coarse to fine resolution
- Atlas-trained references are huge and well-annotated

Weaknesses:

- Reference must match the tissue — a PBMC reference will mis-label tumor cells
- Cells outside the reference manifold get low mapping scores but still get labeled — must filter on `mapping.score`

## 4.3 Reading Azimuth's mapping score

`mapping.score` reflects how well the cell's local neighborhood projects into the reference's manifold. Bimodal distributions are common: one peak is in-reference cells (mapping score 0.7–1.0), the other is out-of-reference cells (0.0–0.4).

For cells with low mapping score:

- The label assignment is unreliable — don't trust it
- These are usually cells whose biology isn't in the reference (e.g. tumor cells when the reference is PBMC)

In an `ifnb` PBMC analysis with the `pbmcref` reference, mapping scores should be uniformly high (>0.7) because the data is the reference's intended target. In a tumor analysis using `pbmcref`, the immune cells score high and the tumor/stromal cells score low — and you should *not* trust the labels on the low-scoring cells.

Always report the share of low-mapping-score cells in your methods, the same way you would report a prune fraction. Healthy on a matched reference: very few. A large low-scoring fraction means the reference doesn't cover your data — pick a different one.

## 4.4 Other reference-based methods you'll meet

The workshop runs Azimuth, but two other paradigms are common in the literature — worth recognizing even though we don't install them here.

**SingleR — correlation against a reference** [@aran2019singler]. SingleR compares each query cell's expression vector against per-label reference profiles by Spearman correlation, then assigns the best-matching label, optionally with a fine-tuning step. Its classic pairing is with the `celldex` package, which provides curated bulk references (e.g. `HumanPrimaryCellAtlasData()`, covering 37 main human cell types). Each call returns a `delta` diagnostic (top-label score minus median) and `pruneScores()` NAs the labels where `delta` is too small. SingleR shines when you only have a *bulk* reference; its weakness, like all reference methods, is that without a confidence threshold every cell gets a label — including cells whose biology isn't in the reference.

**CellTypist — pre-trained logistic regression** [@dominguezconde2022crosstissue]. CellTypist trains logistic-regression models per cell type from atlas-scale references, then runs the trained model on your query. It is fast, returns per-cell probabilities for every label (multi-label aware), and has the largest pre-trained model library. The Python implementation is the mature one; for Python-native (`scanpy`) workflows it is often the first choice.

All three degrade the same way when the reference doesn't cover the query — the benchmarks in §4.1 make that point repeatedly. The practical lesson is identical regardless of method: read the confidence diagnostic, and reconcile against manual markers.

## 4.5 The reconciliation step

After running an automatic method, you have:

- Manual labels (`celltype_manual` from Chapter 3, or the author-curated `seurat_annotations` ground truth)
- Azimuth labels (`predicted.celltype.l2`)

A workable reconciliation rule: **keep the manual label where the automatic call agrees, flag the rest**:

```r
seu$celltype_final <- with(seu@meta.data, dplyr::case_when(
  celltype_manual == predicted.celltype.l2    ~ celltype_manual,
  TRUE                                         ~ paste0("REVIEW:", celltype_manual)
))
```

Cells where the automatic method agrees with manual get the manual label; the rest get a `REVIEW:` prefix and you investigate manually. (If you also ran a second method such as SingleR, add a clause so a cell that agrees with *either* method is accepted.)

For the `ifnb` workflow this typically agrees on 92%+ of cells — the disagreements concentrate in:

- T-cell subsets (CD4 Naive vs Memory)
- Rare populations (pDC, Mk, Eryth)
- Activated states (T activated, B activated) where lineage and state coexist

These are exactly the cells that benefit from human review.

## 4.6 The Cell Ontology connection

For final labels, store the ontology term ID alongside the human label:

```r
seu$cell_type_ontology_term_id <- case_when(
  seu$celltype_final == "B"          ~ "CL:0000236",
  seu$celltype_final == "CD14 Mono"  ~ "CL:0001054",
  ...
)
```

This is required for CELLxGENE submission [@czi2025cellxgene] (see [Tutorial 17](../Exercise_Folder/Tutorial_17_FAIR_Metadata.html)) and makes your dataset interoperable with atlas resources built on cell-type ontologies [@osumisutherland2021cell]. Look up IDs at [OLS](https://www.ebi.ac.uk/ols4/).

## 4.7 Common errors

| You see | What's wrong | What to do |
|---|---|---|
| `RunAzimuth` errors during reference download | Partial cache or no internet | Delete `~/.cache/Azimuth/`, confirm connectivity, retry |
| Mapping scores are uniformly low | Wrong reference for your tissue | Match tissue; consider an atlas like Tabula Sapiens |
| All cells get a coarse label | Using `predicted.celltype.l1` | Use `predicted.celltype.l2` / `l3` for finer resolution |
| Manual and Azimuth disagree on T-cell subsets | Normal — T-cell subdivision is genuinely hard | Inspect markers; document; sometimes both are partly right |

## 4.8 Where this material is also discussed

- **Lecture:** [Lecture 04](../Lecture_Folder/Lecture_04_Reference_Annotation.html)
- **Tutorial:** [Tutorial 04](../Exercise_Folder/Tutorial_04_Reference_Annotation.html)
- **Manual annotation (the complement):** [Chapter 3](Chapter_03_Markers_Annotation.html)
- **Cell Ontology lookup:** [Appendix E § 1.2](Appendix_E_Databases_FileFormats.html), [Tutorial 17](../Exercise_Folder/Tutorial_17_FAIR_Metadata.html)

## 4.9 Going further

Go to the method papers — Azimuth / Seurat v4 [@hao2021integrated], and for the other paradigms SingleR [@aran2019singler] and CellTypist [@dominguezconde2022crosstissue] — and to the two surveys that put them in context: the benchmark of 22 automated classifiers [@abdelaal2019comparison] and the review of 32 annotation methods [@pasquini2021automated]. For hands-on guidance, the **[Azimuth web tool](https://azimuth.hubmapconsortium.org/)** is the practical reference; large atlases such as Tabula Sapiens [@tabulasapiens2022] make good general-purpose references when a tissue-specific one is unavailable. The full curated list is on [Key Papers, Reviews & Benchmarks](../Key_Papers.html).

## References

::: {#refs}
:::
